#ifndef PACKET_HPP
#define PACKET_HPP

#include <qbytearray.h>
#include <qmetaobject.h>

typedef QByteArray Packet;
typedef quint8 ID;
typedef QDataStream Reader;
typedef QDataStream Writer;

#endif // PACKET_HPP
